# NetSync Gov — Variables d'environnement production
# IMPORTANT : Ne jamais committer ce fichier dans Git (.gitignore)

# ── Base de données ──
DB_PASSWORD=CHANGE_ME_strong_password_here

# ── Redis ──
REDIS_PASSWORD=CHANGE_ME_redis_password

# ── JWT ──
JWT_SECRET_KEY=CHANGE_ME_at_least_64_chars_random_string

# ── Resend (email) ──
RESEND_API_KEY=re_CHANGE_ME

# ── WhatsApp Business API (Meta) ──
WHATSAPP_API_TOKEN=CHANGE_ME
WHATSAPP_PHONE_ID=CHANGE_ME
WHATSAPP_WABA_ID=CHANGE_ME

# ── Claude API (parsing fallback) ──
ANTHROPIC_API_KEY=sk-ant-CHANGE_ME

# ── CinetPay ──
CINETPAY_SITE_ID=CHANGE_ME
CINETPAY_API_KEY=CHANGE_ME
CINETPAY_SECRET_KEY=CHANGE_ME

# ── Admin ──
ADMIN_EMAIL=admin@netsync.bf

# ── Domaines ──
DOMAIN=gov.netsync.bf
API_DOMAIN=api.gov.netsync.bf
